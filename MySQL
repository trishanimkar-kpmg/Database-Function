import mysql.connector
class mysql():
    def __init__(self,db1):
        self.dbl = dbl

        self.db= mysql.connector.connect(user='scott', password='password',
                              host='127.0.0.1',
                              database='employees')
    def read(self,table):
        self.table=table
        my_database = mydb1.cursor()
        sql_statement = "SELECT * FROM table"
        my_database.execute(sql_statement)
        output = my_database.fetchall()
        for x in output:
             print(x)
